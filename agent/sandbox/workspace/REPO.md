# Repo

Canonical GitHub: https://github.com/nazarprytula1-code/ops-agent
Default branch: `main`

This sandbox is seeded at deploy time. Game source is under `/workspace/game/` (same tree as `game/` in git).
Primary Universe `10720619997`, Place `81136506656442`. Reserve `10735659349`, Place `113088894576682` (see `game/open-cloud.json`). Push/`main` publishes the **reserve** place.
Slack ships via `open_game_pr` then `merge_game_pr` (HITL). GitHub Actions publishes; this sandbox does not.
Do not ask the human for org/repo. Read files here. On GitHub mentions, prefer the channel checkout if it exists.
